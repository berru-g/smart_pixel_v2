# smart_pixel_v2/.htaccess
RewriteEngine On

# 1. Rediriger la racine vers /public
RewriteCond %{REQUEST_URI} ^/smart_pixel_v2/?$
RewriteRule ^ /smart_pixel_v2/public/ [R=301,L]

# 2. Bloquer l'accès aux fichiers sensibles
<FilesMatch "\.(sql|env|log|bak|ini)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# 3. Empêcher le listing des dossiers
Options -Indexes

# 4. Autoriser l'accès normal à tout le reste
# Rien d'autre - le reste fonctionne normalement